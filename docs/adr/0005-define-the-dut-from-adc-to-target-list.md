---
status: accepted
---

# Define the DUT from ADC samples to a target list

The DUT receives real 16-bit ADC samples for each channel and covers DDC and
decimation, fast-time and slow-time processing, CFAR, simple clustering, and
conversion to a target list. Exact algorithms, rates, units, output fields,
and detection performance targets remain open until specified separately.
